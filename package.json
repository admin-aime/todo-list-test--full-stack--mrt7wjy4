{
  "name": "todo-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:client\"",
    "dev:api": "npm --prefix api run dev",
    "dev:client": "npm --prefix apps/todo-app run dev",
    "build": "npm run build:api && npm run build:client",
    "build:api": "npm --prefix api run build",
    "build:client": "npm --prefix apps/todo-app run build",
    "start": "npm run start:api",
    "start:api": "npm --prefix api run start",
    "preview": "npm --prefix apps/todo-app run preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
